diff --git a/.github/workflows/ci-cd.yml b/.github/workflows/ci-cd.yml
index 80168f0..9b7806f 100644
--- a/.github/workflows/ci-cd.yml
+++ b/.github/workflows/ci-cd.yml
@@ -32,6 +32,10 @@ jobs:
         uses: actions/checkout@v4
 
       - name: Run Infrastructure & Integration Tests via Docker Compose
+        env:
+          DB_USER: myuser
+          DB_PASSWORD: mypassword
+          JWT_SECRET: UneCleSuperSecrèteEtTresLonguePourLeHachageDuJwt123456!
         run: |
           docker compose up --build --exit-code-from api.tests
         continue-on-error: true
diff --git a/.gitignore b/.gitignore
index 603e0fb..21f443a 100644
--- a/.gitignore
+++ b/.gitignore
@@ -367,3 +367,4 @@ FodyWeavers.xsd
 
 # User documents (uploads)
 **/user_documents/
+.env
diff --git a/docker-compose.yml b/docker-compose.yml
index 8ac8615..0b55e98 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -19,9 +19,9 @@ services:
       context: .
       dockerfile: DataBasePostgreSql/Dockerfile
     environment:
-      - DB_USERS_CONNECTION=Host=collectorshop-users-postgres;Port=5432;Database=users_db;Username=myuser;Password=mypassword;
-      - DB_FINANCE_CONNECTION=Host=collectorshop-finance-postgres;Port=5432;Database=finance_db;Username=myuser;Password=mypassword;
-      - DB_COLLECTOR_CONNECTION=Host=collectorshop-collector-postgres;Port=5432;Database=collector_db;Username=myuser;Password=mypassword;
+      - DB_USERS_CONNECTION=Host=collectorshop-users-postgres;Port=5432;Database=users_db;Username=${DB_USER};Password=${DB_PASSWORD};
+      - DB_FINANCE_CONNECTION=Host=collectorshop-finance-postgres;Port=5432;Database=finance_db;Username=${DB_USER};Password=${DB_PASSWORD};
+      - DB_COLLECTOR_CONNECTION=Host=collectorshop-collector-postgres;Port=5432;Database=collector_db;Username=${DB_USER};Password=${DB_PASSWORD};
     depends_on:
       users.database:
         condition: service_healthy
@@ -41,7 +41,7 @@ services:
       - StorageSettings__StorageFolder=/app/user_documents
       - Security__CookieName=CollectorShop_Auth_TK
       - Security__JwtAudience=CollectorShop_SPA
-      - Security__JwtSecret=UneCleSuperSecrèteEtTresLonguePourLeHachageDuJwt123456!
+      - Security__JwtSecret=${JWT_SECRET}
     ports:
       - "5000:5000"
       - "5001:5001"
@@ -65,8 +65,8 @@ services:
     container_name: collectorshop-users-postgres
     restart: always
     environment:
-      POSTGRES_USER: myuser
-      POSTGRES_PASSWORD: mypassword
+      POSTGRES_USER: ${DB_USER}
+      POSTGRES_PASSWORD: ${DB_PASSWORD}
       POSTGRES_DB: users_db
     ports:
       - "5432:5432"
@@ -86,8 +86,8 @@ services:
     container_name: collectorshop-collector-postgres
     restart: always
     environment:
-      POSTGRES_USER: myuser
-      POSTGRES_PASSWORD: mypassword
+      POSTGRES_USER: ${DB_USER}
+      POSTGRES_PASSWORD: ${DB_PASSWORD}
       POSTGRES_DB: collector_db
     ports:
       - "5433:5432"
@@ -107,8 +107,8 @@ services:
     container_name: collectorshop-finance-postgres
     restart: always
     environment:
-      POSTGRES_USER: myuser
-      POSTGRES_PASSWORD: mypassword
+      POSTGRES_USER: ${DB_USER}
+      POSTGRES_PASSWORD: ${DB_PASSWORD}
       POSTGRES_DB: finance_db
     ports:
       - "5434:5432"
@@ -158,4 +158,4 @@ volumes:
   users_postgres_data:
   collector_postgres_data:
   finance_postgres_data:
-  csharp_document_storage:
\ No newline at end of file
+  csharp_document_storage:
